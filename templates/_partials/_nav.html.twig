<nav class="navbar navbar-expand-xl sticky-top app-navbar">
    <div class="container-fluid px-3 px-lg-4 px-xxl-5">
        <a class="navbar-brand app-brand d-flex align-items-center gap-3 me-4" href="{{ path('app_home') }}">
            <div class="lh-sm">
                <div class="app-brand-kicker">Relais Medical</div>
                <div class="app-brand-title">Plateforme commerciale</div>
                <div class="app-brand-subtitle">Pilotage, terrain et decisionnel</div>
            </div>
        </a>

        <div class="d-flex align-items-center gap-2 order-xl-3">
            <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavigation" aria-controls="mainNavigation" aria-expanded="false" aria-label="Afficher la navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            {% if app.user %}
                <button class="btn app-menu-trigger d-inline-flex align-items-center justify-content-center" type="button" data-bs-toggle="offcanvas" data-bs-target="#userMenuPanel" aria-controls="userMenuPanel">
                    <i class="bi bi-sliders2 fs-5"></i>
                </button>
            {% endif %}
        </div>

        <div class="collapse navbar-collapse order-xl-2" id="mainNavigation">
            <ul class="navbar-nav mx-auto mb-3 mb-xl-0 app-nav-menu">
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DIRECTION') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') == 'app_dashboard' ? 'active' : '' }}" href="{{ path('app_dashboard') }}">
                            <i class="bi bi-grid-1x2 fs-5"></i>
                            <span class="small mt-1">Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_stats_' ? 'active' : '' }}" href="{{ path('app_stats_index') }}">
                            <i class="bi bi-pie-chart fs-5"></i>
                            <span class="small mt-1">Stat</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DIRECTION') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_client_' ? 'active' : '' }}" href="{{ path('app_client_index') }}">
                            <i class="bi bi-buildings fs-5"></i>
                            <span class="small mt-1">Clients</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_visit_' ? 'active' : '' }}" href="{{ path('app_visit_index') }}">
                            <i class="bi bi-calendar-check fs-5"></i>
                            <span class="small mt-1">Visites</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_offer_' ? 'active' : '' }}" href="{{ path('app_offer_index') }}">
                            <i class="bi bi-receipt fs-5"></i>
                            <span class="small mt-1">Offres</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_commercial_' ? 'active' : '' }}" href="{{ path('app_commercial_index') }}">
                            <i class="bi bi-people fs-5"></i>
                            <span class="small mt-1">Commerciaux</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DIRECTION') or is_granted('ROLE_COMMERCIAL') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_tour_' ? 'active' : '' }}" href="{{ path('app_tour_index') }}">
                            <i class="bi bi-signpost-split fs-5"></i>
                            <span class="small mt-1">{{ is_granted('ROLE_COMMERCIAL') and not is_granted('ROLE_ADMIN') and not is_granted('ROLE_DIRECTION') ? 'Mes tournees' : 'Tournees' }}</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DIRECTION') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_market_' ? 'active' : '' }}" href="{{ path('app_market_index') }}">
                            <i class="bi bi-bar-chart fs-5"></i>
                            <span class="small mt-1">Marche</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_objective_' ? 'active' : '' }}" href="{{ path('app_objective_index') }}">
                            <i class="bi bi-bullseye fs-5"></i>
                            <span class="small mt-1">Objectifs</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_DIRECTION') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_offer_' ? 'active' : '' }}" href="{{ path('app_offer_index') }}">
                            <i class="bi bi-receipt fs-5"></i>
                            <span class="small mt-1">Offres</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_supplier_' ? 'active' : '' }}" href="{{ path('app_supplier_index') }}">
                            <i class="bi bi-box-seam fs-5"></i>
                            <span class="small mt-1">Fournisseurs</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_product_' ? 'active' : '' }}" href="{{ path('app_product_index') }}">
                            <i class="bi bi-capsule-pill fs-5"></i>
                            <span class="small mt-1">Produits</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_ACHAT') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_supply_order_' ? 'active' : '' }}" href="{{ path('app_supply_order_index') }}">
                            <i class="bi bi-truck fs-5"></i>
                            <span class="small mt-1">Imports</span>
                        </a>
                    </li>
                {% endif %}

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_LOGISTIQUE') %}
                    <li class="nav-item">
                        <a class="nav-link rounded-4 px-3 py-2 d-flex flex-column align-items-center text-center app-nav-link {{ app.request.attributes.get('_route') starts with 'app_delivery_' ? 'active' : '' }}" href="{{ path('app_delivery_index') }}">
                            <i class="bi bi-truck-flatbed fs-5"></i>
                            <span class="small mt-1">Livraisons</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% if app.user %}
    <div class="offcanvas offcanvas-end" tabindex="-1" id="userMenuPanel" aria-labelledby="userMenuPanelLabel">
        <div class="offcanvas-header px-4 pt-4 pb-3 border-bottom">
            <div>
                <div class="app-section-kicker text-secondary mb-1">Espace utilisateur</div>
                <h2 class="h4 mb-0" id="userMenuPanelLabel">Mon compte</h2>
            </div>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Fermer"></button>
        </div>
        <div class="offcanvas-body p-4 d-flex flex-column gap-4">
            <div class="app-user-summary rounded-5 p-4">
                <div class="d-flex align-items-center gap-3 mb-3">
                    <div class="app-user-avatar d-inline-flex align-items-center justify-content-center">
                        {{ app.user.fullName|split(' ')|map(v => v|first)|join('')|slice(0, 2)|upper }}
                    </div>
                    <div class="min-w-0">
                        <div class="fw-bold text-dark">{{ app.user.fullName }}</div>
                        <div class="small text-body-secondary text-break">{{ app.user.userIdentifier }}</div>
                    </div>
                </div>
                <div class="small text-body-secondary">Roles actifs</div>
                <div class="fw-semibold">{{ app.user.roles|join(' • ') }}</div>
            </div>

            <div class="d-grid gap-3">
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('app_settings_index') }}" class="app-user-panel-link">
                        <span class="app-user-panel-icon"><i class="bi bi-sliders"></i></span>
                        <span>
                            <span class="d-block fw-semibold">Parametrage</span>
                            <span class="small text-body-secondary">Villes, zones et choix de reference</span>
                        </span>
                    </a>
                {% endif %}

                <a href="{{ path('app_home') }}" class="app-user-panel-link">
                    <span class="app-user-panel-icon"><i class="bi bi-house-door"></i></span>
                    <span>
                        <span class="d-block fw-semibold">Accueil</span>
                        <span class="small text-body-secondary">Retour au point d'entree de l'application</span>
                    </span>
                </a>
            </div>

            <div class="mt-auto">
                <a href="{{ path('app_logout') }}" class="btn btn-danger w-100 py-3">
                    <i class="bi bi-box-arrow-right me-2"></i>
                </a>
            </div>
        </div>
    </div>
{% endif %}
